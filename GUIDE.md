# dsh-memory 使用指南

> 面向日常使用的名词解释与操作说明。安装/配置开发细节见 [README.md](README.md)。

---

## 一、先建立心智模型：四层记忆

| 层 | 名字 | 是什么 | 类比 |
|---|---|---|---|
| **L0** | 会话事实源 | 你的每轮对话原文，只增不改，按会话存文件 | 原始日记 |
| **L1** | 原子记忆 | 从对话中蒸馏出的一条条独立事实（"用户偏好 pnpm"） | 卡片笔记 |
| **L2** | 场景 | 攒够一批相关记忆后浓缩成的一个主题块（"某项目的负载均衡调研"） | 章节摘要 |
| **L3** | 画像 | 定期更新的长期画像（个人画像 / 工作准则） | 人物档案 |

记忆分为**两个域（族）**：`chat`（个人生活）与 `work`（工作）。L1/L2/L3 全部分域隔离，去重永不跨族——聊项目的记忆不会混进个人画像，反之亦然。

模型每轮回答前，插件自动把相关记忆以 `<recalled-memory>` 注入；长期稳定的画像和场景索引则以 `<user-persona>` / `<scene-navigation>` 常驻上下文。

---

## 二、输入栏的记忆芯片

聊天输入框工具栏上的 `● 记忆 · 智能 ▼` 胶囊就是它，**作用于当前会话**。

### 1. 芯片外观怎么读

- **文字** = 当前会话生效的档位：`记忆 · 智能 / 日常 / 工作 / 暂停`
- **`·只写` 后缀** = 数据流被设为"只写"（见下文）
- **圆点颜色**：
  - 🔘 灰 = 跟随全局默认档（没有单独设置过）
  - 🔵 蓝 = 本会话覆盖了档位（智能/日常）
  - 🟠 橙 = 本会话覆盖为"工作"
  - 🔴 红 = 暂停
- 未发送过消息的新会话芯片半透明——发第一条消息后即可设置。

### 2. 记忆档位（决定"记忆写到哪、从哪读"）

| 菜单项 | 档位 | 蒸馏时 | 召回时 | 适合 |
|---|---|---|---|---|
| **跟随全局** | 清除覆盖 | 用设置页"默认记忆档位" | 同左 | 大多数会话（默认就是智能） |
| **智能** | auto | 双族混合对话，逐条记忆自动判断归"个人"还是"工作" | 跨两个域检索 | 一会儿聊工作一会儿闲聊的混合会话 |
| **日常** | chat | 强制归个人域，只抽生活语境 | 只检索个人域 | 私人聊天，不想混入工作记忆 |
| **工作** | work | 强制归工作域，抽项目/决策/方法/交付物 | 只检索工作域 | 专注干活的会话 |
| **暂停** | off | 完全隐身：不写 L0、不蒸馏、不注入 | 全部静默，读工具也拒答 | 敏感内容、临时糊弄的会话 |

- 切到"暂停"时会记住暂停前的档位，切回任意非暂停档即恢复。
- 修改**即时生效**：切档瞬间，攒着没蒸馏的对话切片先按旧档落袋，新对话按新档走。

### 3. 数据流（只管"读"，与档位正交）

| 菜单项 | 含义 |
|---|---|
| **跟随全局** | 按设置页"召回注入"总开关决定本会话读不读 |
| **读写** | 即使全局召回关着，本会话也强制注入记忆 |
| **只写（沉淀但不注入）** | 对话**照常捕获、照常蒸馏**成记忆，但本会话不注入、画像/场景索引不显示、记忆读工具拒答 |

**一句话区分**：档位管"记忆属于哪个世界（个人/工作/两边/隐身）"，数据流管"这个会话自己能不能看到记忆"。典型用法：想让 AI 帮你写点不想被记住的东西 → 暂停；想沉淀记忆但不想被干扰 → 只写。

---

## 三、设置页五区详解

设置 → 记忆，共五个标签页。所有改动点"保存配置"即时生效，无需重启。

### 总览

- **状态灯**：绿=正常，黄=有告警（下方显示最近告警内容）。
- **记忆资产计数**：记忆总数及个人/工作分域计数、场景数、画像是否已生成、会话数。
- **待蒸馏 N 条**：攒够阈值还没蒸馏的对话消息数；正常会周期性清零。
- **最近活动**：每条记忆的 新增/更新 × 记忆/场景/画像 × 个人/工作 徽章与时间。

### 记忆库

- **资产类型**：L1 记忆（可删除）/ L2 场景 / L3 画像（只读展示）。
- 可按**个人/工作分域**筛选、按关键词搜索；条目点开可看全文、复制、删除。
- 删除某条 L1 记忆不会删除它在 L0 的原始对话。

### 自动化

**开关组**：

| 开关 | 关掉会怎样 |
|---|---|
| 总开关 | 一切暂停（数据保留） |
| L0 捕获 | 对话不再落盘，后续层全部断粮 |
| L1 抽取 | 有捕获但不蒸馏记忆 |
| L2 场景整合 | 记忆照常产生，不再汇总成场景块 |
| L3 画像蒸馏 | 画像停止更新 |
| 召回注入 | 全局读闸门（各会话可用数据流"读写"强制越过） |
| 向量检索 | 关闭 hybrid，退回纯关键词检索 |
| 记忆工具 | 模型失去 memory_search 等三件工具 |

**默认记忆档位**：新会话的出厂档位（智能/日常/工作），已有会话在下方列表单独覆盖。

**会话记忆档位**：最近会话列表，每行可单独设档位与数据流，含义与输入栏芯片完全一致；显示各会话"待蒸馏 N"。

**蒸馏路由链**（进阶）：

- **全局回退链**：主路由（默认跟随你的对话模型）失败时按行依次降级。每行格式 `provider|model[|思考档]`，如 `opencode-go|deepseek-v4-flash|low`。
- 什么算"失败"：请求报错、中途掐断、**输出为空**，都会降级到下一行。
- 每条路由各享**全额**蒸馏超时（不是共享一个）。
- **L1/L2/L3 独立链**：给某一层配专属模型（如 L3 画像用更好的模型）。留空跟随全局；填了且头行 provider|model 齐全则整条替换。
- 每次尝试（含失败）都记入洞察页账本。

**语义检索（嵌入源）**：

- 填一个 OpenAI 兼容 `/embeddings` 服务（如硅基流动 `https://api.siliconflow.cn/v1` + `BAAI/bge-m3`），检索自动升级为 **hybrid**：关键词与向量两路各自召回，RRF 融合排序。
- `维度` 必须与模型实际输出一致，否则该路失败自动降级关键词，不影响使用。
- **重建向量索引**：换模型/改维度后点它全量重嵌（有进度，可取消）；**清空向量** 只删缓存不删记忆。
- 状态行：`已索引 N 条`、`源已切换待重嵌`、`熔断中`（连续失败 60 秒内直接走关键词，稍后自愈）。

**知识库索引**：

- 填本地目录（如 Obsidian 库），插件把 md/txt 内容按段落分块索引——自动跳过 `.obsidian`/`.git` 等噪音目录与超 2MB 的文件；保存后每 10 分钟增量扫描（按文件改动时间判断，几乎零开销），也可点"立即扫描"。
- 效果：召回时命中**文件内容片段**（带相对路径），模型直接拿到内容而不是只知道位置——根治"记住了知识库位置却不读"。
- 片段与 L1 记忆独立去重（同会话不重复注入）；每轮片段上限（默认 3）与总字符预算（默认 1200）在卡片里调。
- 纯关键词评分（阈值比记忆宽松）；配置嵌入源后文件片段不做向量索引（个人库规模下关键词已够）。

**蒸馏与召回参数**：

| 参数 | 含义 | 动它的时机 |
|---|---|---|
| 抽取阈值（条消息） | 攒多少条消息触发一次 L1 抽取（1→2→4 爬坡到此上限） | 想更勤快/更省蒸馏 |
| 闲置兜底（秒） | 闲聊停止 N 秒后把尾巴蒸馏掉 | 常聊完就走就调小 |
| 召回条数上限 | 每轮最多注入几条记忆 | 嫌 injected 太多/太少 |
| 召回分数阈值 | 低于此分不注入（0.2~0.4 常见） | 召回太杂就调高 |
| 注入总字符上限 | `<recalled-memory>` 总预算 | 想省上下文 |
| 召回超时 | 超过即本轮放弃注入，不阻塞对话 | — |
| 时效半衰期（天） | 老记忆分数按半衰期衰减，0=关闭 | 想更看重新记忆 |
| 蒸馏超时 | 单次蒸馏调用的超时 | 慢模型调大 |
| 蒸馏输入预算 | 单次喂给蒸馏的字符上限，超限自动分块抽取 | — |

### 洞察

- **筛选**：时间窗（近 1~365 天）× 粒度（天/周/月）× 层级（L1/L2/L3）。
- **趋势图**：按模型分色的输出 token 折线；柱图为每日新增记忆与蒸馏调用次数。
- **召回与会话**：累计注入轮次/条数、会话暂停/只写分布。
- **调用明细表**：每模型的调用/失败次数、输出与思考 token、**均值/中位数**单次输出、输入字符量——用来判断哪个模型蒸馏最省最稳。
- 层级分布与近期失败原因也在本页。

### 维护

- **全量重建**：丢弃现有 L1–L3，从 L0 原始对话重新蒸馏（统一按智能档归族）。L0 不受影响。换蒸馏模型/改 Prompt 后想重整记忆时用；有进度可取消。
- **诊断日志**：info 级以上滚动日志，排障第一入口（更完整的宿主级错误在桌面端 `logs\host\` 目录）。
- **危险区**：清空记忆资产（L0 保留）/ 连 L0 一起删除（二次确认，不可恢复）。

---

## 四、高频问题

- **改了配置要重启吗？** 不用，设置页保存即时生效；只有改插件代码才需要重启。
- **为什么这轮没注入记忆？** 按顺序查：总开关/召回注入开着吗 → 会话是暂停或只写吗（芯片）→ 洞察页召回计数涨没涨 → 阈值是不是太高。
- **两个域的记忆会互相污染吗？** 不会，L1/L2/L3 分域隔离、去重不跨族；只有智能档召回会跨域读。
- **记忆记住了我的知识库/目录位置，AI 为什么没读里面的内容？** 记忆系统只记录**对话里说过的话**，不索引文件——"知识库位于 C:\..." 是一条指针型记忆，位置≠内容。插件已做三层防护：①注入的记忆里出现位置线索时，自动附带"先用文件工具读取实际内容再回答"的提示；②画像稳定区常驻 `<memory-usage>` 规则；③**自动化 → 知识库索引**填入目录后，文件内容本身会变成可检索资产、召回时直接注入相关片段（推荐，一劳永逸）。若希望强制读取，请求里直接写明，如"先读一遍 C:\...\knowledge 再回答"。
- **换嵌入模型后要做什么？** 改模型与维度 → 点"重建向量索引"；期间检索自动走关键词，无感。
- **数据都在哪？** `~/.dsh/dsh-memory/`，纯 JSON/JSONL 文件，可备份可迁移；与原版 dsh-layered-memory 的 `~/.dsh/memory/` 互不干扰。
